import 'package:flutter/material.dart';

class ManageFriends extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Friends')),
      body: Center(child: Text('Friend management screen')),
    );
  }
}
